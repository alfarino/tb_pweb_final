<aside class="w-64 bg-white shadow-lg">
  <!-- Header Profil -->
  <div class="p-6 border-b border-gray-200">
    <div class="flex items-center space-x-3">
      <div class="w-10 h-10 bg-black rounded-full"></div>
      <span class="text-gray-700 font-semibold">
        <%= user?.fullName || '(Nama Akun)' %>
      </span>
    </div>
  </div>

  <!-- Menu Navigasi -->
  <nav class="mt-4 space-y-1">
    <% if (user && user.isAdmin) { %>

      <a href="/admin/adminprofile"
         class="flex items-center space-x-3 px-6 py-3 font-medium hover:bg-gray-100
                <%= currentUrl === '/admin/adminprofile' ? 'bg-gray-100 text-blue-600' : 'text-gray-700' %>">
        <span>👤</span>
        <span>Akun Saya</span>
      </a>

      <a href="/admin/dashboard"
         class="flex items-center space-x-3 px-6 py-3 font-medium hover:bg-gray-100
                <%= currentUrl === '/admin/dashboard' ? 'bg-gray-100 text-blue-600' : 'text-gray-700' %>">
        <span>📊</span>
        <span>Dashboard Admin</span>
      </a>

      <a href="/admin/item-approval"
         class="flex items-center space-x-3 px-6 py-3 font-medium hover:bg-gray-100
                <%= currentUrl === '/admin/item-approval' ? 'bg-gray-100 text-blue-600' : 'text-gray-700' %>">
        <span>✅</span>
        <span>Approval Product</span>
      </a>

      <a href="/admin/userapproval"
         class="flex items-center space-x-3 px-6 py-3 font-medium hover:bg-gray-100
                <%= currentUrl === '/admin/userapproval' ? 'bg-gray-100 text-blue-600' : 'text-gray-700' %>">
        <span>👤</span>
        <span>Approval Pengguna</span>
      </a>

      <a href="/admin/database-items"
         class="flex items-center space-x-3 px-6 py-3 font-medium hover:bg-gray-100
                <%= currentUrl === '/admin/database-items' ? 'bg-gray-100 text-blue-600' : 'text-gray-700' %>">
        <span>📦</span>
        <span>Kelola Produk</span>
      </a>

      <a href="/admin/database-users"
         class="flex items-center space-x-3 px-6 py-3 font-medium hover:bg-gray-100
                <%= currentUrl === '/admin/database-users' ? 'bg-gray-100 text-blue-600' : 'text-gray-700' %>">
        <span>🧑‍💼</span>
        <span>Kelola Pengguna</span>
      </a>

      <!-- Kelola Transaksi -->
      <a href="/admin/database-transactions"
         class="flex items-center space-x-3 px-6 py-3 font-medium hover:bg-gray-100
                <%= currentUrl === '/admin/database-transactions' ? 'bg-gray-100 text-blue-600' : 'text-gray-700' %>">
        <span>💳</span>
        <span>Kelola Transaksi</span>
      </a>

    <% } %>
  </nav>
</aside>
