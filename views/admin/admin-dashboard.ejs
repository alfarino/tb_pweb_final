<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Dashboard Admin - CampusExchange</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">

  <!-- Navbar -->
<% if (user?.isAdmin) { %>
  <%- include('adminnavbar') %>
<% } else { %>
  <%- include('layout/navbar') %>
<% } %>

  <!-- Layout Grid -->
  <div class="flex flex-col md:flex-row flex-1">
    <!-- Sidebar -->
    <%- include('./adminsidebar') %>

    <!-- Main Content -->
    <main class="flex-1 p-10 overflow-y-auto">
      <div class="bg-white rounded-xl shadow-md p-8">
        <h1 class="text-3xl font-bold text-gray-800 mb-4">Dashboard Admin</h1>
        <p class="text-gray-600 mb-6">Selamat datang di halaman admin!</p>

        <!-- Statistik -->
        <h2 class="text-2xl font-semibold text-gray-700 mb-4">Statistik</h2>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <div class="bg-white border border-gray-200 rounded-lg p-6 shadow-sm">
            <h3 class="text-gray-700 font-medium mb-2">Total Pengguna</h3>
            <p class="text-3xl font-bold text-blue-600">0</p>
          </div>

          <div class="bg-white border border-gray-200 rounded-lg p-6 shadow-sm">
            <h3 class="text-gray-700 font-medium mb-2">Total Produk</h3>
            <p class="text-3xl font-bold text-blue-600">0</p>
          </div>

          <div class="bg-white border border-gray-200 rounded-lg p-6 shadow-sm">
            <h3 class="text-gray-700 font-medium mb-2">Total Pesanan</h3>
            <p class="text-3xl font-bold text-blue-600">0</p>
          </div>
        </div>
      </div>
    </main>
  </div>

  <!-- Footer -->
  <%- include('../layout/footer') %>
</body>
</html>
