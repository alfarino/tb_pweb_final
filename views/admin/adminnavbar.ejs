<!-- Navbar Admin -->
<nav class="bg-gradient-to-r from-green-700 to-green-500 text-white px-6 py-3 shadow-md flex justify-between items-center">
  <!-- Logo -->
  <div class="font-bold text-lg flex items-center">
    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
    </svg>
    <span class="tracking-wide">CampusExchange Admin</span>
  </div>

  <!-- Search Bar -->
  <form action="/search" method="GET" class="flex-grow max-w-xl mx-6">
    <div class="relative">
      <input
        type="text"
        name="q"
        placeholder="Cari produk..."
        class="w-full px-4 py-2 rounded-full border-none text-black focus:outline-none focus:ring-2 focus:ring-green-500 shadow-sm"
      />
      <button type="submit" class="absolute right-2 top-1/2 transform -translate-y-1/2 text-green-600">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
        </svg>
      </button>
    </div>
  </form>

  <!-- Menu & Profil -->
  <div class="flex items-center space-x-4">
    <!-- Menu -->
    <a href="/" class="px-4 py-2 rounded-md hover:bg-green-800 bg-white text-green-600 hover:text-white font-semibold transition shadow-sm flex items-center">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3" />
      </svg>
      Home
    </a>
    <a href="/product" class="px-4 py-2 rounded-md hover:bg-green-800 bg-white text-green-600 hover:text-white font-semibold transition shadow-sm flex items-center">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
      </svg>
      Produk
    </a>
    <a href="/contact" class="px-4 py-2 rounded-md hover:bg-green-800 bg-white text-green-600 hover:text-white font-semibold transition shadow-sm flex items-center">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 12H8m8 0a4 4 0 00-8 0m8 0a4 4 0 01-8 0" />
      </svg>
      Kontak
    </a>

    <!-- Profil -->
    <div class="relative">
      <button id="profileBtn" class="flex items-center focus:outline-none">
        <img src="<%= user?.profileImage || '/images/default-profile.png' %>" alt="Foto Profil"
          class="w-10 h-10 rounded-full border-2 border-white shadow-md hover:scale-110 transition object-cover" />
        <span class="ml-2 hidden md:inline text-white">
          <%= user?.fullName?.split(' ')[0] || 'Admin' %>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 inline ml-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
          </svg>
        </span>
      </button>

      <!-- Dropdown -->
      <div id="dropdownMenu"
        class="hidden absolute right-0 mt-3 w-64 bg-white text-gray-800 rounded-xl shadow-2xl z-50 overflow-hidden transform transition-all duration-300 scale-95 opacity-0">
        <!-- Arrow -->
        <div class="absolute -top-2 right-4 w-4 h-4 bg-white rotate-45 shadow-md z-0"></div>

        <!-- Header -->
        <div class="flex items-center gap-3 px-5 py-4 border-b border-gray-200 bg-gray-50">
          <div class="w-12 h-12 bg-green-600 text-white rounded-full flex items-center justify-center font-bold text-lg shadow-sm">
            <%= user?.fullName?.charAt(0) || 'A' %>
          </div>
          <div class="text-sm">
            <p class="font-bold text-gray-800 leading-tight"><%= user?.fullName || 'Admin' %></p>
            <p class="text-xs text-gray-500"><%= user?.email || 'admin@example.com' %></p>
          </div>
        </div>

        <!-- Menu -->
        <a href="/admin/adminprofile" class="flex items-center gap-3 px-5 py-3 text-sm font-medium hover:bg-gray-100 transition
          <%= currentUrl === '/admin/adminprofile' ? 'bg-gray-100 text-blue-600' : 'text-gray-700' %>">
          <div class="w-8 h-8 rounded-full bg-blue-100 text-blue-600 flex items-center justify-center">👤</div>
          <span>Akun Saya</span>
        </a>
        <a href="/admin/dashboard" class="flex items-center gap-3 px-5 py-3 text-sm font-medium hover:bg-gray-100 transition
          <%= currentUrl === '/admin/dashboard' ? 'bg-gray-100 text-blue-600' : 'text-gray-700' %>">
          <div class="w-8 h-8 rounded-full bg-indigo-100 text-indigo-600 flex items-center justify-center">📊</div>
          <span>Dashboard Admin</span>
        </a>
        <a href="/admin/item-approval" class="flex items-center gap-3 px-5 py-3 text-sm font-medium hover:bg-gray-100 transition
          <%= currentUrl === '/admin/item-approval' ? 'bg-gray-100 text-blue-600' : 'text-gray-700' %>">
          <div class="w-8 h-8 rounded-full bg-green-100 text-green-600 flex items-center justify-center">✅</div>
          <span>Approval Item</span>
        </a>
        <a href="/admin/user-approval" class="flex items-center gap-3 px-5 py-3 text-sm font-medium hover:bg-gray-100 transition
          <%= currentUrl === '/admin/user-approval' ? 'bg-gray-100 text-blue-600' : 'text-gray-700' %>">
          <div class="w-8 h-8 rounded-full bg-yellow-100 text-yellow-600 flex items-center justify-center">🧑‍💼</div>
          <span>Approval Pengguna</span>
        </a>
        <hr class="my-1 border-gray-200">
        <a href="/logout" class="flex items-center gap-3 px-5 py-3 text-sm font-medium text-red-600 hover:bg-red-50 transition">
          <div class="w-8 h-8 rounded-full bg-red-100 text-red-600 flex items-center justify-center">🚪</div>
          <span>Keluar</span>
        </a>
      </div>
    </div>
  </div>
</nav>

<!-- Script Dropdown -->
<script>
  document.addEventListener('DOMContentLoaded', function () {
    const profileBtn = document.getElementById('profileBtn');
    const dropdownMenu = document.getElementById('dropdownMenu');

    profileBtn.addEventListener('click', function (e) {
      e.preventDefault();
      dropdownMenu.classList.toggle('hidden');
      setTimeout(() => {
        dropdownMenu.classList.toggle('opacity-0');
        dropdownMenu.classList.toggle('scale-95');
      }, 10);
    });

    window.addEventListener('click', function (e) {
      if (!profileBtn.contains(e.target) && !dropdownMenu.contains(e.target)) {
        dropdownMenu.classList.add('opacity-0');
        dropdownMenu.classList.add('scale-95');
        setTimeout(() => {
          dropdownMenu.classList.add('hidden');
        }, 200);
      }
    });
  });
</script>
